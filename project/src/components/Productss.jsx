import React, { useContext } from 'react'
import { productContext } from '../context/ProductContextProvider'

export default function Productss() {
    let {prod, setProd} = useContext(productContext)

  return (
    <div>
        
    </div>
  )
}
